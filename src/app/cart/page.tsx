import { DefaultPageLayout } from "@/components/default-page-layout";

export default async function CartPage() {

    return (
        <DefaultPageLayout>
            <div>cart</div>
        </DefaultPageLayout>
    );
}
